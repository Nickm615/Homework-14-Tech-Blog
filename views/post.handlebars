{{!-- {{post}} --}}
<section class = 'card'>
    <header><a href='/post/{{post.id}}'>{{post.title}}</a></header>
    <p>{{post.content}}</p>
    <p>Created by {{post.user.name}} at {{format_date post.createdAt}}</p>
    {{!-- console.log({{created_at}}) --}}
    <a href="/comments/{{post.id}}"class='btn-block'>Leave a comment</a>
</section>